# KA Lineage

MAIOS Conversation Kernel uses a public derivative of relations developed in
KA — Kernel Assiomatico.

The accepted source event used for the initial projection is identified by
revision:

```text
d6cd550d6b68449271c912f78d885b129bdf1f7c
```

The delivered relations are:

- source before synthesis;
- difference before convergence;
- evidence and invalidator before promotion;
- transfer the invariant and leave local residue;
- reversible evolution with effect authority separate;
- present field including the possibility horizon;
- improvement demonstrated by resulting behavior rather than self-description.

This repository does not contain the private KA repository, node contribution
lanes, Lab state, imported source archive, hidden reasoning, credentials, host
thresholds or runtime authority. It transfers an attributed invariant into an
owner-native Project Kernel incarnation.

KA and KA-Core remain distinct: the source system may evolve or replace its
current kernel component. This derivative is therefore source-linked and
revisable, not a claim to be KA itself.
