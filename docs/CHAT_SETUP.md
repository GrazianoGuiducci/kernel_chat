# Compatibility path — chat setup

The canonical setup guide is now:

[AI interaction setup](INTERACTION_SETUP.md)

A chat remains one important host for `kernel_chat`, but chat/conversation is
not the parameter that defines the carrier. Use this compatibility path only to
follow older links; new setup and adoption documentation should use
`INTERACTION_SETUP.md`.
