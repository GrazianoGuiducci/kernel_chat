# First Packet

```text
objective:
  operate and evolve a user-owned open-world conversational kernel without a
  designed capability ceiling;

source:
  CURRENT_STATE.md, KERNEL.md, sources/bootstrap/SOURCE_ATLAS_v1.0.md and only
  the closest owner-native source made relevant by the present task;

boundary:
  capability, installation, activation and effect authority remain separate;
  no external effect is granted by this packet;

first_safe_action:
  reconstruct the active present and use the current conversation directly
  unless a missing source relation changes the result;

memory delta:
  preserve only accepted changes that alter later behavior, proof, recovery or
  reentry, or protect a non-reconstructible relation or possibility.
```

The current capability field is open. If an unrepresented relation matters,
retain it source-bound, compose existing faculties and create a falsifiable
owner-native candidate only when a real gap remains.
