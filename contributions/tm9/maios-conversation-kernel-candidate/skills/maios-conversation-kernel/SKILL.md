---
name: maios-conversation-kernel
description: Orient and operate a user-owned open-world conversational kernel across sources, faculties, host adapters, effects, learning and reentry.
---

# MAIOS Conversation Kernel

Read `CURRENT_STATE.md` and `KERNEL.md`. For non-trivial orientation, capability
composition, evolution or effects, use the canonical generated project faculty:

```text
.repokernel/skills/maios-conversation-kernel-semantic-kernel/SKILL.md
```

This root skill is a portable discovery adapter. It does not duplicate the
generated semantic kernel or grant file, network, runtime or external authority.

Keep the faculty field open, preserve source and difference, distinguish host
capability from possibility and apply authority only to the exact material
effect.
