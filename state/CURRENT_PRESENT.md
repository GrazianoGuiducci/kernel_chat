# Portable Current Present

Status: working candidate state
Updated: 2026-08-20
State owner: this fork/repository

This is the compact semantic/cognitive reentry surface for the portable working incarnation. It is not the root project `CURRENT_STATE.md` and it is not a copy of external project truth.

## Accepted present

```text
working_incarnation:
  first portable all-in-one conversational system candidate

current purpose:
  exercise one fork-owned package that can support direct conversation,
  selective semantic reentry, situated competence, operational continuity,
  reversible evolution and replaceable host adapters

kernel maturity:
  candidate_only

behavioral activation:
  unverified

portability:
  unverified
```

## Current relations

```text
ordinary work:
  use current conversation/working set directly when sufficient

semantic reentry:
  use this file only when durable state can change the result

operational reentry:
  operations/CURRENT_STATE.md only when unfinished causal work matters

cognitive core:
  kernel/KERNEL.md

routing:
  kernel/ROUTING.md

competence:
  kernel/COMPETENCE.md

learning/evolution:
  evolution/EVOLUTION.md and evolution/CRYSTALLIZATION.md when material

host binding:
  adapters/<host>/ only for current host mechanics/capability
```

## User/project bindings

No external semantic/project source is bound by default.

A fork may add compact pointers here when a project/source materially participates in recurring work. Do not copy the external project's current state merely for convenience.

```text
project/source bindings: none
```

## Material unknowns

```text
- whether the minimal core improves real work without adding latency;
- whether operational continuity can remain cold until needed;
- whether a fresh ChatGPT instance can reenter from this fork without private state;
- which relations survive a second host incarnation;
- whether current organ boundaries simplify or become unnecessary through use.
```

## Active operational relation

```text
none
```

If a flow becomes active, record only the pointer/summary required to know that `operations/CURRENT_STATE.md` can change continuation. Keep operational detail in the operational organ.

## Active low-latency learning

See `state/ACTIVE.md` only when a durable relation has earned active attention. No user-specific active condensate is installed by default.

## Recheck

Update this file when a verified result changes what a later compatible conversation must recover. Do not append chronology merely because work occurred.
